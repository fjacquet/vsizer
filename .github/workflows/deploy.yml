name: Deploy
on:
  push: { branches: [main] }
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  deploy:
    uses: fjacquet/ci/.github/workflows/web-deploy.yml@v1
    with: { node-version: "24", build-dir: dist }
